#configuration for the MCP Command Server
[command_blocking]
# List of command substrings that will be blocked if found in the user's input command.
prohibited_commands = [
    "rm ",
    "mv ",
    "sudo ",
    "su "
]

# If set to True, all command restrictions are ignored and execution is permitted.
override = false 

#override = true 
